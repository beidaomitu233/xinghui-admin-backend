[配置](#/%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8?id=%e9%85%8d%e7%bd%ae)
------------------------------------------------------------------

重要提示：

如果是旧版本升级需要注意：从 2.0.0 版本开始，因为更换了项目名称及结构，导致依赖名称、配置文件参数名及包名与之前版本有所不同，详情查看 [更新记录](#/更新记录?id=_200)

`pom.xml` 引入本项目，这里默认是 `SpringBoot` 环境，`Solon` 环境参考 [在 Solon 中使用](#/在Solon中使用)，其它环境参考 [脱离 SpringBoot 单独使用](#/脱离SpringBoot单独使用)

```
<dependency>
    <groupId>org.dromara.x-file-storage</groupId>
    <artifactId>x-file-storage-spring</artifactId>
    <version>2.3.0</version>
</dependency>
```

再引入对应平台的依赖，用不到平台不用引入

```
<dependency>
    <groupId>com.huaweicloud</groupId>
    <artifactId>esdk-obs-java</artifactId>
    <version>3.22.12</version>
</dependency>
```

```
<dependency>
    <groupId>com.aliyun.oss</groupId>
    <artifactId>aliyun-sdk-oss</artifactId>
    <version>3.16.1</version>
</dependency>
```

```
<dependency>
    <groupId>com.qiniu</groupId>
    <artifactId>qiniu-java-sdk</artifactId>
    <version>7.12.1</version>
</dependency>
```

```
<dependency>
    <groupId>com.qcloud</groupId>
    <artifactId>cos_api</artifactId>
    <version>5.6.137</version>
</dependency>
```

```
<dependency>
    <groupId>com.baidubce</groupId>
    <artifactId>bce-java-sdk</artifactId>
    <version>0.10.251</version>
</dependency>
```

```
<dependency>
    <groupId>com.upyun</groupId>
    <artifactId>java-sdk</artifactId>
    <version>4.2.3</version>
</dependency>
```

```
<dependency>
    <groupId>io.minio</groupId>
    <artifactId>minio</artifactId>
    <version>8.5.2</version>
</dependency>
```

根据 Amazon 公告，AWS SDK for Java 1.x 自 2024 年 7 月 31 日起将进入维护模式，并于 2025 年 12 月 31 日终止支持  
公告链接地址：[The AWS SDK for Java 1.x is in maintenance mode, effective July 31, 2024](https://aws.amazon.com/blogs/developer/the-aws-sdk-for-java-1-x-is-in-maintenance-mode-effective-july-31-2024/)

若要使用第三方兼容的存储平台，使用这种方式（1.x）兼容性会好些，否则请使用 Amazon S3 V2 版本

```
<dependency>
    <groupId>com.amazonaws</groupId>
    <artifactId>aws-java-sdk-s3</artifactId>
    <version>1.12.429</version>
</dependency>


<dependency>
    <groupId>com.squareup.okhttp3</groupId>
    <artifactId>okhttp</artifactId>
    <version>4.12.0</version>
</dependency>
```

适用于AWS SDK for Java 2.x，根据 Amazon 公告，AWS SDK for Java 1.x 自 2024 年 7 月 31 日起将进入维护模式，并于 2025 年 12 月 31 日终止支持  
公告链接地址：[The AWS SDK for Java 1.x is in maintenance mode, effective July 31, 2024](https://aws.amazon.com/blogs/developer/the-aws-sdk-for-java-1-x-is-in-maintenance-mode-effective-july-31-2024/)

若要使用第三方兼容的存储平台，建议使用 Amazon S3 （1.x）的方式，兼容性会好些

```
<dependency>
    <groupId>software.amazon.awssdk</groupId>
    <artifactId>s3</artifactId>
    <version>2.29.29</version>
</dependency>
```

```
<dependency>
    <groupId>commons-net</groupId>
    <artifactId>commons-net</artifactId>
    <version>3.9.0</version>
</dependency>


<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-extra</artifactId>
    <version>5.8.28</version>
</dependency>


<dependency>
    <groupId>org.apache.commons</groupId>
    <artifactId>commons-pool2</artifactId>
    <version>2.11.1</version>
</dependency>
```

```
<dependency>
    <groupId>com.jcraft</groupId>
    <artifactId>jsch</artifactId>
    <version>0.1.55</version>
</dependency>


<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-extra</artifactId>
    <version>5.8.28</version>
</dependency>


<dependency>
    <groupId>org.apache.commons</groupId>
    <artifactId>commons-pool2</artifactId>
    <version>2.11.1</version>
</dependency>
```

```
<dependency>
    <groupId>com.github.lookfirst</groupId>
    <artifactId>sardine</artifactId>
    <version>5.10</version>
</dependency>
```

```
<dependency>
    <groupId>com.google.cloud</groupId>
    <artifactId>google-cloud-storage</artifactId>
    <version>2.20.1</version>
</dependency>

<dependency>
    <groupId>com.google.guava</groupId>
    <artifactId>guava</artifactId>
    <version>31.1-jre</version>
</dependency>
```

使用前请先参考 [兼容性说明-FastDFS](#/存储平台?id=oci_fastdfs)

```
<dependency>
    <groupId>io.github.rui8832</groupId>
    <artifactId>fastdfs-client-java</artifactId>
    <version>1.30-20230328</version>
</dependency>
```

使用前请先参考 [兼容性说明-Azure Blob Storage](#/存储平台?id=oci_azureblobstorage)

```
<dependency>
    <groupId>com.azure</groupId>
    <artifactId>azure-storage-blob</artifactId>
    <version>12.23.1</version>
</dependency>

<dependency>
    <groupId>com.azure</groupId>
    <artifactId>azure-storage-file-datalake</artifactId>
    <version>12.18.1</version>
</dependency>
```

使用前请先参考 [兼容性说明-Mongo GridFS](#/存储平台?id=oci_mongogridfs)

```

<dependency>
    <groupId>org.mongodb</groupId>
    <artifactId>mongodb-driver-sync</artifactId>
    <version>4.6.1</version>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-mongodb</artifactId>
</dependency>
```

使用前请先参考 [兼容性说明-go-fastdfs](#/存储平台?id=oci_gofastdfs)

```

<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-http</artifactId>
    <version>5.8.28</version>
</dependency>

<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-json</artifactId>
    <version>5.8.28</version>
</dependency>
```

```
<dependency>
    <groupId>com.volcengine</groupId>
    <artifactId>ve-tos-java-sdk</artifactId>
    <version>2.8.8</version>
</dependency>
```

无需依赖

无需依赖

`application.yml` 配置文件中先添加以下基础配置

```
dromara:
  x-file-storage: 
    default-platform: huawei-obs-1 
    thumbnail-suffix: ".min.jpg" 
    
```

再添加对应平台的配置，不使用的情况下可以不写

关于配置文件及 FileInfo 中各种路径（path）的区别，可以参考 [常见问题](#/常见问题?id=%e9%85%8d%e7%bd%ae%e6%96%87%e4%bb%b6%e5%8f%8a-fileinfo-%e4%b8%ad%e5%90%84%e7%a7%8d%e8%b7%af%e5%be%84%ef%bc%88path%ef%bc%89%e7%9a%84%e5%8c%ba%e5%88%ab%ef%bc%9f)

```
huawei-obs:
  - platform: huawei-obs-1 
    enable-storage: true  
    access-key: ??
    secret-key: ??
    end-point: ??
    bucket-name: ??
    domain: ?? 
    base-path: test/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringHuaweiObsConfig`

```
aliyun-oss:
  - platform: aliyun-oss-1 
    enable-storage: true  
    access-key: ??
    secret-key: ??
    end-point: ??
    bucket-name: ??
    domain: ?? 
    base-path: test/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringAliyunOssConfig`

```
qiniu-kodo:
  - platform: qiniu-kodo-1 
    enable-storage: true  
    access-key: ??
    secret-key: ??
    bucket-name: ??
    domain: ?? 
    base-path: base/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringQiniuKodoConfig`

```
tencent-cos:
  - platform: tencent-cos-1 
    enable-storage: true  
    secret-id: ??
    secret-key: ??
    region: ?? 
    bucket-name: ??
    domain: ?? 
    base-path: test/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringTencentCosConfig`

```
baidu-bos:
  - platform: baidu-bos-1 
    enable-storage: true  
    access-key: ??
    secret-key: ??
    end-point: ?? 
    bucket-name: ??
    domain: ?? 
    base-path: test/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringBaiduBosConfig`

```
upyun-uss:
  - platform: upyun-uss-1 
    enable-storage: true  
    username: ??
    password: ??
    bucket-name: ??
    domain: ?? 
    base-path: test/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringUpyunUssConfig`

```
minio:
  - platform: minio-1 
    enable-storage: true  
    access-key: ??
    secret-key: ??
    end-point: ??
    bucket-name: ??
    domain: ?? 
    base-path: test/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringMinioConfig`

可以在存储桶的属性中看到 AWS 区域，这里是亚太地区(香港)对应是：ap-east-1 ，桶名称是：abcd，按照以下替换成你的信息即可

```
amazon-s3: 
  - platform: amazon-s3-1 
    enable-storage: true  
    access-key: ??
    secret-key: ??
    region: ap-east-1 
    end-point: https://s3.ap-east-1.amazonaws.com/ 
    bucket-name: abcd 
    domain: https://abcd.s3.ap-east-1.amazonaws.com/ 
    base-path: s3/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringAmazonS3Config`

可以在存储桶的属性中看到 AWS 区域，这里是亚太地区(香港)对应是：ap-east-1 ，桶名称是：abcd，按照以下替换成你的信息即可

```
amazon-s3-v2: 
  - platform: amazon-s3-v2-1 
    enable-storage: true  
    access-key: ??
    secret-key: ??
    region: ap-east-1 
    end-point: https://s3.ap-east-1.amazonaws.com/ 
    bucket-name: abcd 
    domain: https://abcd.s3.ap-east-1.amazonaws.com/ 
    base-path: s3/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringAmazonS3Config`

```
ftp:
  - platform: ftp-1 
    enable-storage: true  
    host: ?? 
    port: 21 
    user: anonymous 
    password: "" 
    domain: ?? 
    base-path: config/ 
    storage-path: /www/wwwroot/file.abc.com/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringFtpConfig`

```
sftp: 
  - platform: sftp-1 
    enable-storage: true  
    host: ?? 
    port: 22 
    user: root 
    password: ?? 
    private-key-path: ?? 
    domain: ?? 
    base-path: sftpConfig/ 
    storage-path: /www/wwwroot/file.abc.com/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringSftpConfig`

```
webdav:
  - platform: webdav-1 
    enable-storage: true  
    server: ?? 
    user: ?? 
    password: ?? 
    domain: ?? 
    base-path: webdav/ 
    storage-path: / 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringWebDavConfig`

```
google-cloud-storage: 
  - platform: google-cloud-storage-1 
    enable-storage: true  
    project-id: ?? 
    bucket-name: ??
    credentials-path: file:/deploy/example-key.json 
    domain: ?? 
    base-path: test/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringGoogleCloudStorageConfig`

FastDFS 比较特殊，不支持自定义路径及文件名，所以提供了 run-mod 参数来解决这个问题，详情阅读 [兼容性说明-FastDFS](#/存储平台?id=oci_fastdfs) 章节

```
fastdfs:
  - platform: fastdfs-1 
    enable-storage: true  
    run-mod: COVER 
    tracker-server: 
      server-addr: ?? 
    domain: ?? 
    base-path: "" 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringFastDfsConfig`

注意在 Azure 控制台创建存储帐户时要勾选“启用分层命名空间”功能，上传成功后的 `url` 默认是无法公开访问的，详情阅读 [兼容性说明-AzureBlobStorage](#/存储平台?id=oci_azureblobstorage) 章节

```
azure-blob:
  - platform: azure-blob-1 
    enable-storage: true  
    connection-string: ?? 
    end-point: ?? 
    container-name: ?? 
    domain: ?? 
    base-path: hy/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringAzureBlobStorageConfig`

使用时有几点需要注意，详情阅读 [兼容性说明-MongoGridFS](#/存储平台?id=oci_mongogridfs) 章节

```
mongo-grid-fs:
  - platform: mongo-grid-fs-1 
    enable-storage: true  
    connection-string: mongodb://admin:123456@192.168.1.111:27017
    database: abc 
    bucket-name: ccd 
    domain: https://abc.com/ 
    base-path: hy/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringMongoGridFsConfig`

注意，在 SpringBoot 环境下，当检测到 Mongo 相关依赖时，SpringBoot 会自动初始化它自己的 Mongo 服务，如果没有相关配置会报错，但实际上不影响使用，你可以写上相关配置来防止报错

```
spring:
  data:
    mongodb:
      uri: mongodb://admin:123456@192.168.1.111:27017
```

注意go-fastdfs删除、详情等功能需要开启服务端的白名单后才能使用

```
go-fastdfs:
  - platform: go-fastdfs-1 
    enable-storage: true 
    server:  
    group: 'group1' 
    scene: 'scene' 
    time-out: 30000 
    domain: '' 
    ramdom: true
    base-path: '' 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringGoFastDfsConfig`

```
volcengine-tos: 
  - platform: volcengine-tos-1 
    enable-storage: true  
    access-key: ??
    secret-key: ??
    region: ?? 
    end-point: ?? 
    bucket-name: ?? 
    domain: ?? 
    base-path: tos/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringVolcengineTosConfig`

已不推荐使用，建议使用本地升级版

```
local:
  - platform: local-1 
    enable-storage: true  
    enable-access: true 
    domain: "" 
    base-path: D:/Temp/test/ 
    path-patterns: /test/file/** 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringLocalConfig`

```
local-plus:
  - platform: local-plus-1 
    enable-storage: true  
    enable-access: true 
    domain: http://127.0.0.1:8080/file/ 
    base-path: local-plus/ 
    path-patterns: /file/** 
    storage-path: D:/Temp/ 
```

更多参数请参考 `org.dromara.x.file.storage.spring.SpringFileStorageProperties.SpringLocalPlusConfig`

注意配置每个平台前面都有个`-`号，通过以下方式可以配置多个，最终的配置文件格式应该如下

```
dromara:
  x-file-storage: 
    default-platform: huawei-obs-1 
    thumbnail-suffix: ".min.jpg" 
    huawei-obs:
      - platform: huawei-obs-1 
        enable-storage: true  
        access-key: ??
        secret-key: ??
        end-point: ??
        bucket-name: ??
        domain: ?? 
        base-path: test/ 
      - platform: huawei-obs-2 
        enable-storage: true  
        access-key: ??
        secret-key: ??
        end-point: ??
        bucket-name: ??
        domain: ?? 
        base-path: test2/ 
    aliyun-oss:
      - platform: aliyun-oss-1 
        enable-storage: true  
        access-key: ??
        secret-key: ??
        end-point: ??
        bucket-name: ??
        domain: ?? 
        base-path: test/ 
```

查看 [所有支持的存储平台](#/存储平台)

[编码](#/%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8?id=%e7%bc%96%e7%a0%81)
------------------------------------------------------------------

在启动类上加上`@EnableFileStorage`注解

```
@EnableFileStorage
@SpringBootApplication
public class SpringFileStorageTestApplication {

    public static void main(String[] args) {
        SpringApplication.run(SpringFileStorageTestApplication.class,args);
    }

}
```

[开始使用](#/%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8?id=%e5%bc%80%e5%a7%8b%e4%bd%bf%e7%94%a8)
--------------------------------------------------------------------------------------

### [上传](#/%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8?id=%e4%b8%8a%e4%bc%a0)

支持 File、MultipartFile、UploadedFile、byte\[\]、InputStream、URL、URI、String、HttpServletRequest，大文件会自动分片上传。如果想支持更多方式，请阅读 [文件适配器](#/文件适配器) 章节

```
@RestController
public class FileDetailController {

    @Autowired
    private FileStorageService fileStorageService;

    
    @PostMapping("/upload")
    public FileInfo upload(MultipartFile file) {
        return fileStorageService.of(file).upload();
    }
    
    
    @PostMapping("/upload2")
    public String upload2(MultipartFile file) {
        FileInfo fileInfo = fileStorageService.of(file)
                .setPath("upload/") 
                .setSaveFilename("image.jpg") 
                .setObjectId("0")   
                .setObjectType("0") 
                .putAttr("role","admin") 
                .upload();  
        return fileInfo == null ? "上传失败！" : fileInfo.getUrl();
    }

    
    @PostMapping("/upload-image")
    public FileInfo uploadImage(MultipartFile file) {
        return fileStorageService.of(file)
                .image(img -> img.size(1000,1000))  
                .thumbnail(th -> th.size(200,200))  
                .upload();
    }

    
    @PostMapping("/upload-platform")
    public FileInfo uploadPlatform(MultipartFile file) {
        return fileStorageService.of(file)
                .setPlatform("aliyun-oss-1")    
                .upload();
    }

    
    @PostMapping("/upload-request")
    public FileInfo uploadPlatform(HttpServletRequest request) {
        return fileStorageService.of(request).upload();
    }
}
```

### [其它操作](#/%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8?id=%e5%85%b6%e5%ae%83%e6%93%8d%e4%bd%9c)

```

FileInfo fileInfo = new FileInfo()
        .setPlatform("huawei-obs-1")
        .setBasePath("test/")
        .setPath("aa/")
        .setFilename("image.png")
        .setThFilename("image.png.min.jpg");


boolean exists = fileStorageService.exists(fileInfo);

byte[] bytes = fileStorageService.download(fileInfo).bytes();

fileStorageService.delete(fileInfo);


```

如果将文件记录保存到数据库中，还可以更方便的根据 URL 进行操作了，详情请阅读 [保存上传记录](#/基础功能?id=%e4%bf%9d%e5%ad%98%e4%b8%8a%e4%bc%a0%e8%ae%b0%e5%bd%95) 章节

```

FileInfo fileInfo = fileStorageService.getFileInfoByUrl("https://abc.def.com/test/aa/image.png");


boolean exists = fileStorageService.exists("https://abc.def.com/test/aa/image.png");

byte[] bytes = fileStorageService.download("https://abc.def.com/test/aa/image.png").bytes();

fileStorageService.delete("https://abc.def.com/test/aa/image.png");

```

[我的博客（常年不更新）](https://xuyanwu.cn/)        [用 JavaScript 在浏览器中控制台观看 BadApple!!](https://app.xuyanwu.cn/BadApple/)